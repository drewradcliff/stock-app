export type Stock = {
  id: number;
  symbol: string;
  name: string;
  marketCap: string;
};

export type History = {
  id: number;
  date: string;
  open: string;
  high: string;
  low: string;
  close: string;
  volume: string;
};
